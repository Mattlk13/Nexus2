{
  "name": "nexus-backend",
  "version": "1.0.0",
  "description": "NEXUS AI Social Marketplace Backend",
  "main": "server.py",
  "scripts": {
    "start": "uvicorn server:fastapi_app --host 0.0.0.0 --port ${PORT:-8001}",
    "dev": "uvicorn server:fastapi_app --host 0.0.0.0 --port 8001 --reload"
  },
  "engines": {
    "python": "3.11.x"
  }
}
